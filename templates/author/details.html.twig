<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Author Details -
			{{ author.name }}</title>
		<link rel="stylesheet" href="{{ asset('node_modules/bootstrap/dist/css/bootstrap.min.css') }}">
		<style>
			body {
				font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
				background-color: #f0f2f5;
				color: #333;
			}

			.card {
				max-width: 900px;
				margin: 40px auto;
				border: none;
				border-radius: 15px;
				overflow: hidden;
				background-color: #fff;
				box-shadow: 0 12px 20px rgba(0, 0, 0, 0.1);
			}

			.card-header {
				background-color: #4e73df;
				color: white;
				padding: 1.5rem;
				text-align: center;
			}

			.card-title {
				font-size: 1.75rem;
				font-weight: bold;
			}

			.author-img {
				width: 180px;
				height: 180px;
				border-radius: 50%;
				object-fit: cover;
				margin-bottom: 1rem;
				box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
			}

			.card-body {
				padding: 2rem;
			}

			.card-body h3 {
				font-size: 1.4rem;
				font-weight: bold;
				margin-bottom: 1rem;
			}

			.card-body p {
				font-size: 1rem;
				margin-bottom: 0.75rem;
			}

			.book-list {
				margin-top: 1.5rem;
			}

			.book-list h4 {
				font-size: 1.3rem;
				font-weight: bold;
				margin-bottom: 0.75rem;
			}

			.list-group-item {
				display: flex;
				justify-content: space-between;
				padding: 0.75rem 1.5rem;
				font-size: 1rem;
			}

			.list-group-item .badge {
				background-color: #4e73df;
				padding: 0.5rem 1rem;
				font-size: 0.85rem;
				border-radius: 15px;
			}

			.btn {
				text-decoration: none;
			}

			.btn-group {
				display: flex;
				justify-content: space-between;
				margin-top: 1.5rem;
			}

			.btn-custom {
				flex: 1;
				padding: 12px;
				font-size: 1rem;
				border-radius: 30px;
				border: none;
				margin-right: 10px;
				transition: all 0.3s ease;
			}

			.btn-custom:last-child {
				margin-right: 0;
			}

			.btn-show {
				background-color: #4e73df;
				color: white;
			}

			.btn-edit {
				background-color: #f6c23e;
				color: white;
			}

			.btn-custom:hover {
				opacity: 0.9;
			}

			@media(max-width: 768px) {
				.card-body {
					padding: 1.5rem;
				}

				.btn-group {
					flex-direction: column;
				}

				.btn-custom {
					margin-bottom: 10px;
				}

				.btn-custom:last-child {
					margin-bottom: 0;
				}
			}
		</style>
	</head>

	<body>
		<div class="container">
			<div
				class="card">
				<!-- Header con el nombre del autor -->
				<div class="card-header">
					<h2 class="card-title">{{ author.name }}</h2>
				</div>

				<!-- Cuerpo de la tarjeta con la información del autor -->
				<div class="card-body">
					<div
						class="text-center">
						<!-- Imagen del autor -->
						<img src="{{ asset(author.image) }}" alt="{{ author.name }}" class="author-img">
					</div>

					<!-- Información del autor -->
					<h3>Author Information</h3>
					<p>
						<strong>Email:</strong>
						{{ author.email }}</p>
					<p>
						<strong>Number of Books:</strong>
						{{ books_count }}</p>

					<!-- Lista de libros -->
					<div class="book-list">
						<h4>Books:</h4>
						<ul class="list-group">
							{% for book in author.books %}
								<li class="list-group-item">
									{{ book }}
									<span class="badge">View</span>
								</li>
							{% endfor %}
						</ul>
					</div>

					<!-- Botones de acción -->
					<div class="btn-group">
						<a href="{{ path('app_author_list') }}" class="btn btn-custom btn-show">Show</a>
						<a href="{{ path('author_edit', {'id': author.id}) }}" class="btn btn-custom btn-edit">Edit</a>
					</div>
				</div>
			</div>
		</div>

		<script src="{{ asset('node_modules/bootstrap/dist/js/bootstrap.bundle.min.js') }}"></script>
	</body>

</html>
